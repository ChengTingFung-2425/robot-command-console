# MCP 服務模組依賴套件
# Note: common deps (aiohttp, passlib, PyJWT, prometheus_client, python-json-logger,
# pyyaml, jsonschema) are in root requirements.txt

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# HTTP 客戶端
httpx>=0.25.0

# 資料驗證
pydantic>=2.5.0

# 資料庫（選用）
sqlalchemy>=2.0.0
alembic>=1.13.0

# Redis（選用）
redis>=5.0.0
aioredis>=2.0.1

# 工具
python-dotenv>=1.0.0
