# PyQt6 核心
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0

# 打包工具
pyinstaller>=6.0.0

# HTTP 請求
# Note: requests and cryptography are in root requirements.txt
urllib3>=2.6.3  # 修復 Dependabot 安全警告：解壓縮炸彈保護繞過漏洞

# SSH/SFTP 支援
paramiko>=3.3.0
scp>=0.14.5

# WiFi 管理（跨平台）
pywifi>=1.1.12

# 進度條顯示
tqdm>=4.66.0

# 注意：運行前需先安裝主專案依賴：
#   pip install -r ../requirements.txt
# 或使用統一安裝：
#   pip install -r ../requirements.txt && pip install -r requirements.txt
