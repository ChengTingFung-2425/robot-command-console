# Phase 3 — ALL-in-One Edge App 規劃文件

> 此文件定義 Phase 3 的 ALL-in-One Edge App 目標、架構設計與整合規劃。
> 
> **建立日期**：2025-11-26  
> **狀態**：📋 規劃中
> 
> **前置條件**：Phase 2 模組化與後端服務層調整

---

## 目錄

1. [Phase 2 成果回顧](#phase-2-成果回顧)
2. [專案目標](#專案目標)
3. [架構設計](#架構設計)
4. [Edge vs Cloud 職責劃分](#edge-vs-cloud-職責劃分)
5. [硬體與作業系統支援](#硬體與作業系統支援)
6. [使用者流程](#使用者流程)
7. [技術整合設計](#技術整合設計)
8. [CLI/TUI 與 Docker 部署](#clitui-與-docker-部署)
9. [模組化與升級策略](#模組化與升級策略)
10. [安全性設計](#安全性設計)
11. [階段目標與驗收條件](#階段目標與驗收條件)
12. [未來擴展](#未來擴展)

---

## Phase 2 成果回顧

Phase 3 建立在 Phase 2 完成的基礎架構上。以下為 Phase 2 的關鍵成果與在 Phase 3 中的延續：

### 模組化重構

| Phase 2 變更 | 說明 | Phase 3 運用 |
|-------------|------|-------------|
| **目錄重組** | Electron 移至 `electron-app/`，測試移至 `tests/` | 繼續沿用，擴充 Edge 專用模組 |
| **共用模組** | 建立 `src/common/`（日誌、時間、配置工具） | 作為 Edge 服務的基礎工具層 |
| **環境配置** | `EdgeConfig` / `ServerConfig` 區分環境 | 擴充 Edge 專用配置項目 |
| **佇列系統** | `src/robot_service/queue/` 本地優先權佇列 | 整合至 Edge 服務層 |

### LLM 提供商整合（Phase 2）

Phase 2 實作的 LLM 提供商管理器（`MCP/llm_provider_manager.py`）將作為 Phase 3 Edge LLM 管理的核心：

```python
# Phase 2 已完成的功能
from MCP.llm_provider_manager import LLMProviderManager

manager = LLMProviderManager()
await manager.discover_providers()  # 自動偵測 Ollama / LM Studio
await manager.select_provider("ollama")
await manager.generate("請機器人向前移動", model="llama2:7b")
```

**Phase 3 將延續**：
- 自動偵測本地 LLM 提供商
- 雲端 LLM 提供商配置
- 動態切換提供商與模型
- 健康監控與回退機制
- 本地 LLM 備援支援

### 插件架構（Phase 2）

Phase 2 實作的插件系統（`MCP/plugin_manager.py`）支援：
- 指令插件（`CommandPlugin`）
- 裝置插件（`DevicePlugin`）
- 整合插件（`IntegrationPlugin`）

**Phase 3 將擴充**：
- 運行時插件熱載入
- 插件版本管理
- 插件市集整合（與雲端共享）

### Server-Edge-Runner 架構

Phase 2 規劃的三層架構將在 Phase 3 完整實作：

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│     Server      │────▶│      Edge       │────▶│     Runner      │
│  (Cloud/MCP)    │     │ (ALL-in-One)    │     │ (Robot-Console) │
│  雲端服務       │     │ 本地處理/佇列   │     │ 機器人執行     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### 相關文件

- [MIGRATION_GUIDE_PHASE2.md](../phase2/MIGRATION_GUIDE_PHASE2.md) - Phase 2 遷移指南
- [MCP_LLM_PROVIDERS.md](../mcp/MCP_LLM_PROVIDERS.md) - LLM 提供商整合
- [MCP_PLUGIN_ARCHITECTURE.md](../mcp/MCP_PLUGIN_ARCHITECTURE.md) - 插件架構
- [ADVANCED_COMMAND_RESPONSIBILITY_CHANGE.md](../phase2/ADVANCED_COMMAND_RESPONSIBILITY_CHANGE.md) - 進階指令職責變更

---

## 專案目標

### 核心願景

將現有的 MCP、WebUI、Robot-Console 三大模組整合為一個統一的「ALL-in-One Edge App」，部署於消費級邊緣運算設備上，為機器人提供完整的本地控制能力。

### 主要目標

1. **統一啟動器**：單一安裝與啟動程式，一鍵部署所有核心服務
2. **LLM 提供商選擇**：使用者可在應用內選擇本地或雲端 LLM 提供商與模型
3. **模組整合**：MCP（協調者）+ WebUI（用戶介面）+ Robot-Console（執行層）無縫整合
4. **邊緣優先**：大部分處理在本地完成，減少雲端依賴
5. **離線支援**：核心功能可在無網路環境下運作
6. **多模式部署**：支援 GUI（Electron）、CLI/TUI、Docker 三種部署模式

### 設計原則

- **Edge-First**：運算與決策優先在本地執行
- **Cloud-Enhanced**：雲端提供增強功能而非核心依賴
- **Modular**：模組化設計，可獨立升級各組件
- **Secure**：安全第一，敏感資料本地存儲
- **User-Centric**：以使用者體驗為中心的設計

---

## 架構設計

### 整體架構圖

```
┌────────────────────────────────────────────────────────────────────────────┐
│                         Cloud Services Layer                                │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────────┐  │
│  │ Shared Advanced  │  │  Discussion &    │  │  Shared LLM Service      │  │
│  │ Command Library  │  │  Community       │  │  (Big Data Analytics)    │  │
│  │                  │  │                  │  │                          │  │
│  │ • 進階指令共享   │  │ • 用戶討論區    │  │ • 指令行為分析          │  │
│  │ • 指令評分排名   │  │ • 經驗分享      │  │ • 最佳化建議            │  │
│  │ • 社群貢獻      │  │ • 用戶授權認證   │  │ • 群體學習優化          │  │
│  └────────┬─────────┘  └────────┬─────────┘  └────────────┬─────────────┘  │
└───────────┼─────────────────────┼────────────────────────┼─────────────────┘
            │                     │                        │
            │ HTTPS/WSS          │                        │
            │ (當網路可用時)      │                        │
            ▼                     ▼                        ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                      Edge ALL-in-One Application                            │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Unified Launcher                              │   │
│  │  • 一鍵啟動/停止所有服務                                              │   │
│  │  • LLM 提供商與模型選擇介面                                           │   │
│  │  • 系統健康監控儀表板                                                 │   │
│  │  • 自動更新檢查                                                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Edge Services                                  │  │
│  │                                                                        │  │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐       │  │
│  │  │   Local WebUI   │  │   MCP Service   │  │ Robot-Console   │       │  │
│  │  │                 │  │                 │  │                 │       │  │
│  │  │ • 用戶設定介面 │  │ • 指令協調     │  │ • 動作執行     │       │  │
│  │  │ • 機器人監控   │  │ • LLM 處理器   │  │ • 感測器整合   │       │  │
│  │  │ • 固件更新     │  │ • 插件系統     │  │ • 安全機制     │       │  │
│  │  │ • 指令記錄     │  │ • 本地佇列     │  │ • MQTT/HTTP    │       │  │
│  │  │ • 離線快取     │  │ • 提供商管理   │  │ • 緊急停止     │       │  │
│  │  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘       │  │
│  │           │                    │                    │                 │  │
│  │  ┌────────┴────────────────────┴────────────────────┴────────┐       │  │
│  │  │              Local Message Bus / Event System              │       │  │
│  │  │              (IPC / WebSocket / In-Memory Queue)          │       │  │
│  │  └───────────────────────────────────────────────────────────┘       │  │
│  │                                                                        │  │
│  │  ┌─────────────────────────────────────────────────────────────┐     │  │
│  │  │                    Local LLM Provider                        │     │  │
│  │  │  (Ollama / LM Studio / Custom - 可選本地或雲端)              │     │  │
│  │  └─────────────────────────────────────────────────────────────┘     │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Local Storage                                  │  │
│  │  • 用戶設定 (SQLite/JSON)                                              │  │
│  │  • 使用的進階指令快取                                                  │  │
│  │  • 指令歷史記錄                                                        │  │
│  │  • LLM 模型檔案（若本地部署）                                          │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ Hardware Interface
                                    │ (MQTT / Serial / ROS)
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Robot Hardware                                  │
│  • 人形機器人、AGV、機械手臂等                                               │
│  • 感測器（IMU、超音波、攝影機）                                             │
│  • 致動器（馬達、舵機）                                                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 模組通訊架構

```
┌────────────────────────────────────────────────────────────┐
│                    Electron Main Process                   │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              Service Orchestrator                     │ │
│  │  • 啟動/停止 Python 服務                              │ │
│  │  • 健康檢查與自動恢復                                 │ │
│  │  • Token 管理                                         │ │
│  └────────────────────────┬─────────────────────────────┘ │
└───────────────────────────┼─────────────────────────────────┘
                            │ spawn/IPC
                            ▼
┌────────────────────────────────────────────────────────────┐
│                Python Service Layer                         │
│                                                             │
│  ┌───────────────┐    ┌───────────────┐    ┌─────────────┐ │
│  │  Flask API    │◄──►│  MCP Core     │◄──►│ Robot Svc   │ │
│  │  (Port 5000)  │    │  (Internal)   │    │ (Queue)     │ │
│  └───────┬───────┘    └───────┬───────┘    └──────┬──────┘ │
│          │                    │                    │        │
│          │    ┌───────────────┴───────────────┐   │        │
│          │    │      Plugin Manager            │   │        │
│          │    │  • LLM Provider Plugins       │   │        │
│          │    │  • Command Plugins            │   │        │
│          │    │  • Device Plugins             │   │        │
│          │    └───────────────────────────────┘   │        │
│          │                                         │        │
│          └──────────────────┬──────────────────────┘        │
│                             │                               │
│                      ┌──────┴──────┐                       │
│                      │ Local Store │                       │
│                      │  (SQLite)   │                       │
│                      └─────────────┘                       │
└────────────────────────────────────────────────────────────┘
```

---

## Edge vs Cloud 職責劃分

根據專案記憶與主計畫，明確定義各層職責：

### Edge（本地）職責

| 功能領域 | 具體職責 | 說明 |
|---------|---------|------|
| **用戶設定** | 本地存儲用戶偏好設定 | 語言、主題、通知設定等 |
| **機器人監控** | 即時狀態監控、健康檢查 | 感測器數據、電池、連線狀態 |
| **固件更新** | 管理機器人固件更新流程 | 下載、驗證、部署固件 |
| **指令介面** | 提供 LLM 解析指令的介面 | 語音/文字轉機器人動作 |
| **進階指令使用記錄** | 記錄本地使用的進階指令 | 離線使用、歷史查詢 |
| **本地佇列** | 優先權佇列、離線緩衝 | 確保指令可靠執行 |
| **LLM 處理** | 本地 LLM 推論（可選） | Ollama/LM Studio |
| **安全控制** | 緊急停止、邊界檢查 | 即時安全機制 |

### Cloud（雲端）職責

| 功能領域 | 具體職責 | 說明 |
|---------|---------|------|
| **進階指令共享** | 社群共享進階指令庫 | 用戶可上傳/下載指令 |
| **討論區** | 用戶經驗分享與討論 | 社群互動平台 |
| **用戶授權** | 集中式身份認證 | OAuth/SSO 整合 |
| **指令排名** | 進階指令評分與排名 | 根據使用量、評價排序 |
| **信任評級** | 用戶信任度評級系統 | 基於貢獻與社群互動 |
| **共享 LLM 服務** | 大數據分析優化的 LLM | 群體智慧優化指令解析 |
| **指令行為分析** | 分析大量指令執行數據 | 提供最佳化建議 |
| **固件倉庫** | 集中管理固件版本 | 提供固件下載服務 |

### 資料同步策略

```
┌─────────────────┐                     ┌─────────────────┐
│      Edge       │                     │     Cloud       │
│                 │                     │                 │
│  ┌───────────┐  │   上傳（授權後）    │  ┌───────────┐  │
│  │ 進階指令  │──┼────────────────────►│  │ 指令庫    │  │
│  │ 使用記錄  │  │                     │  │ 分析系統  │  │
│  └───────────┘  │                     │  └───────────┘  │
│                 │                     │                 │
│  ┌───────────┐  │   下載（需要時）    │  ┌───────────┐  │
│  │ 本地快取  │◄─┼────────────────────│  │ 共享指令  │  │
│  │           │  │                     │  │ 庫         │  │
│  └───────────┘  │                     │  └───────────┘  │
│                 │                     │                 │
│  ┌───────────┐  │   同步（雙向）      │  ┌───────────┐  │
│  │ 用戶設定  │◄─┼────────────────────►│  │ 用戶設定  │  │
│  │ 本地主要  │  │                     │  │ 雲端備份  │  │
│  └───────────┘  │                     │  └───────────┘  │
└─────────────────┘                     └─────────────────┘
```

---

## 硬體與作業系統支援

### 目標硬體平台

| 類別 | 代表設備 | CPU 架構 | 最低規格 | 建議規格 |
|------|---------|----------|---------|---------|
| **Mini-PC** | Intel NUC, Beelink | x86_64 | 4GB RAM, 32GB 儲存 | 8GB RAM, 64GB SSD |
| **邊緣運算** | NVIDIA Jetson (Nano/Xavier) | ARM64 | 4GB RAM | 8GB+ RAM, GPU |
| **單板電腦** | Raspberry Pi 4/5 | ARM64 | 4GB RAM | 8GB RAM |
| **工控主機** | 工業級 IPC | x86_64 | 4GB RAM | 8GB+ RAM |

### 作業系統支援

| 作業系統 | 版本 | 支援狀態 | 備註 |
|---------|------|---------|------|
| **Ubuntu** | 20.04 LTS, 22.04 LTS | ✅ 主要支援 | 推薦用於生產環境 |
| **Debian** | 11 (Bullseye), 12 (Bookworm) | ✅ 支援 | 穩定性優先 |
| **Raspbian/Pi OS** | 64-bit | ✅ 支援 | Raspberry Pi 專用 |
| **NVIDIA JetPack** | 5.x | ✅ 支援 | Jetson 專用 |
| **Windows** | 10, 11 | ⚠️ 有限支援 | 開發/測試用 |
| **macOS** | 12+ (Monterey+) | ⚠️ 有限支援 | 開發/測試用 |

### LLM 本地運行需求

| LLM 方案 | 最低需求 | 建議配置 | 備註 |
|---------|---------|---------|------|
| **Ollama (7B)** | 8GB RAM | 16GB RAM | CPU 推論 |
| **Ollama (7B) GPU** | 8GB VRAM | NVIDIA GPU | CUDA 加速 |
| **LM Studio** | 8GB RAM | 16GB RAM | 多模型支援 |
| **Whisper (語音)** | 4GB RAM | 8GB RAM, GPU | 語音轉文字 |

---

## 使用者流程

### 首次安裝流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          Installation Flow                               │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────┐
│  1. 下載安裝包                                                          │
│     • AppImage (Linux)                                                  │
│     • DMG (macOS)                                                       │
│     • NSIS Installer (Windows)                                          │
│     • Docker Image                                                      │
└────────────────────────────────────────┬──────────────────────────────┘
                                         ▼
┌───────────────────────────────────────────────────────────────────────┐
│  2. 執行安裝精靈                                                        │
│     • 接受授權條款                                                      │
│     • 選擇安裝路徑                                                      │
│     • 選擇組件（完整/精簡/自訂）                                        │
└────────────────────────────────────────┬──────────────────────────────┘
                                         ▼
┌───────────────────────────────────────────────────────────────────────┐
│  3. LLM 配置精靈                                                        │
│     • 選擇 LLM 模式：本地 / 雲端 / 混合                                 │
│     • 若本地：選擇提供商（Ollama / LM Studio）                          │
│     • 若本地：選擇或下載模型                                            │
│     • 若雲端：輸入 API 金鑰                                             │
└────────────────────────────────────────┬──────────────────────────────┘
                                         ▼
┌───────────────────────────────────────────────────────────────────────┐
│  4. 機器人連線配置                                                      │
│     • 掃描本地機器人                                                    │
│     • 手動輸入連線資訊                                                  │
│     • 測試連線                                                          │
└────────────────────────────────────────┬──────────────────────────────┘
                                         ▼
┌───────────────────────────────────────────────────────────────────────┐
│  5. 完成設定，啟動應用                                                  │
│     • 所有服務自動啟動                                                  │
│     • 顯示系統狀態儀表板                                                │
│     • 準備接受指令                                                      │
└───────────────────────────────────────────────────────────────────────┘
```

### 日常使用流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            Daily Usage Flow                              │
└─────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────┐
│  啟動應用                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  • Unified Launcher 啟動                                         │  │
│  │  • 自動啟動所有服務（MCP、Flask、佇列）                           │  │
│  │  • 健康檢查確認所有服務正常                                       │  │
│  │  • 連接已配置的機器人                                             │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────┬──────────────────────────────┘
                                         ▼
┌───────────────────────────────────────────────────────────────────────┐
│  發送指令                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  使用者輸入：                                                     │  │
│  │  • 語音指令（透過 Whisper 轉文字）                               │  │
│  │  • 文字指令（直接輸入）                                           │  │
│  │  • 選擇進階指令（從本地或雲端庫）                                 │  │
│  │                                                                   │  │
│  │  LLM 處理：                                                       │  │
│  │  • 本地 LLM 解析指令為動作序列                                    │  │
│  │  • 或使用雲端 LLM 服務                                            │  │
│  │                                                                   │  │
│  │  執行：                                                           │  │
│  │  • MCP 路由指令到正確的機器人                                     │  │
│  │  • Robot-Console 執行動作序列                                     │  │
│  │  • 即時回饋執行狀態                                               │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────┬──────────────────────────────┘
                                         ▼
┌───────────────────────────────────────────────────────────────────────┐
│  監控與管理                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  • 即時機器人狀態監控                                             │  │
│  │  • 指令執行歷史查看                                               │  │
│  │  • 系統資源使用監控                                               │  │
│  │  • 錯誤日誌與告警                                                 │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 技術整合設計

### MCP + WebUI + Robot-Console 整合

#### 狀態共享機制

```python
# 共享狀態管理架構
class SharedStateManager:
    """
    管理 MCP、WebUI、Robot-Console 之間的共享狀態
    """
    
    def __init__(self):
        self.state_store = LocalStateStore()  # SQLite
        self.event_bus = LocalEventBus()      # In-memory pub/sub
    
    # 機器人狀態（由 Robot-Console 更新，WebUI 讀取）
    async def update_robot_status(self, robot_id: str, status: dict):
        await self.state_store.set(f"robot:{robot_id}:status", status)
        await self.event_bus.publish("robot.status.updated", {
            "robot_id": robot_id,
            "status": status
        })
    
    # 指令佇列狀態（由 MCP 更新，WebUI 顯示）
    async def get_queue_status(self) -> dict:
        return await self.state_store.get("queue:status")
    
    # 用戶設定（WebUI 更新，所有服務讀取）
    async def get_user_settings(self) -> dict:
        return await self.state_store.get("user:settings")
```

#### 通訊協定

| 通訊路徑 | 協定 | 用途 |
|---------|------|------|
| Electron ↔ Python Service | HTTP + Bearer Token | API 呼叫 |
| WebUI ↔ MCP | Internal HTTP | 指令路由 |
| MCP ↔ Robot-Console | Message Queue | 指令派發 |
| Robot-Console ↔ Robot | MQTT / HTTP / Serial | 硬體控制 |
| Edge ↔ Cloud | HTTPS / WSS | 資料同步 |

### LLM 整合設計

#### 提供商抽象層

```python
# 延續 Phase 2 的 LLM 提供商管理器
from MCP.llm_provider_manager import LLMProviderManager

class EdgeLLMManager:
    """
    Edge 環境的 LLM 管理器
    """
    
    def __init__(self):
        self.provider_manager = LLMProviderManager()
        self.fallback_mode = "rule_based"  # 當 LLM 不可用時
    
    async def initialize(self):
        """啟動時自動偵測可用的 LLM 提供商"""
        await self.provider_manager.discover_providers()
        
        # 如果沒有本地提供商，檢查雲端配置
        if not self.provider_manager.get_available_providers():
            await self._check_cloud_provider()
    
    async def process_command(self, text: str, robot_id: str) -> dict:
        """
        處理自然語言指令
        """
        try:
            # 優先使用本地 LLM
            result = await self.provider_manager.generate(text)
            return self._parse_llm_response(result)
        except Exception as e:
            # 回退到規則式解析
            return self._rule_based_parse(text)
```

#### 模型選擇 UI

```
┌─────────────────────────────────────────────────────────────┐
│                    LLM Provider Settings                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ◉ Local LLM Provider                                        │
│    ├─ Provider: [Ollama           ▼]                         │
│    ├─ Model:    [llama2:7b        ▼]                         │
│    └─ Status:   🟢 Running (45ms response time)              │
│                                                              │
│  ○ Cloud LLM Provider                                        │
│    ├─ Provider: [OpenAI           ▼]                         │
│    ├─ API Key:  [••••••••••••••••••]                        │
│    └─ Model:    [gpt-4-turbo      ▼]                         │
│                                                              │
│  ○ Hybrid Mode (Local first, Cloud fallback)                 │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  Available Local Models:                                 │ │
│  │                                                          │ │
│  │  • llama2:7b (3.8 GB)     [Downloaded] ✓               │ │
│  │  • mistral:7b (4.1 GB)    [Downloaded] ✓               │ │
│  │  • codellama:7b (3.8 GB)  [Download]                    │ │
│  │  • phi-2 (2.7 GB)         [Download]                    │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                              │
│  [Test Connection]  [Apply Settings]                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## CLI/TUI 與 Docker 部署

### CLI 模式

適用於無頭（headless）伺服器或容器環境。

```bash
# 安裝
pip install robot-console-edge

# 啟動所有服務
robot-console start --config /path/to/config.yaml

# 僅啟動特定服務
robot-console start mcp --port 8000
robot-console start flask --port 5000
robot-console start queue --workers 5

# 狀態查詢
robot-console status
robot-console status mcp
robot-console status robots

# 發送指令
robot-console send "go forward" --robot robot-001
robot-console execute patrol --robot robot-001

# 停止
robot-console stop
```

### TUI 模式

提供終端機的互動式介面。

```bash
# 啟動 TUI
robot-console tui

# TUI 畫面範例
┌─────────────────────────────────────────────────────────────────────┐
│  Robot Console Edge - Terminal UI                    [q: quit]       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Services                          │ Robot Status                    │
│  ─────────────────────────────    │ ─────────────────────────────  │
│  ● MCP Service     [running]      │ 🤖 robot-001  [connected]      │
│  ● Flask API       [running]      │    Battery: 85%                │
│  ● Queue Service   [running]      │    Mode: Standby               │
│  ○ LLM Provider    [not running]  │                                 │
│                                    │ 🤖 robot-002  [disconnected]  │
│                                    │                                 │
├────────────────────────────────────┴────────────────────────────────┤
│  Command History                                                     │
│  ─────────────────────────────────────────────────────────────────  │
│  [10:23:45] robot-001: go_forward (success)                         │
│  [10:23:48] robot-001: turn_left (success)                          │
│  [10:24:01] robot-001: stand (success)                              │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│  > Enter command: _                                                  │
└─────────────────────────────────────────────────────────────────────┘
```

### Docker 部署

```yaml
# docker-compose.yml
version: '3.8'

services:
  edge-all-in-one:
    image: robot-console/edge:latest
    container_name: robot-console-edge
    ports:
      - "5000:5000"   # Flask API
      - "8000:8000"   # MCP API
      - "8080:8080"   # WebUI
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - LLM_PROVIDER=ollama
      - LLM_HOST=ollama:11434
      - MQTT_BROKER=mqtt-broker:1883
    depends_on:
      - ollama
      - mqtt-broker
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]  # 如果有 GPU
    restart: unless-stopped

  mqtt-broker:
    image: eclipse-mosquitto:2
    container_name: mqtt-broker
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto/config:/mosquitto/config:ro
    restart: unless-stopped

volumes:
  ollama_models:
```

```bash
# 部署指令
docker-compose up -d

# 查看日誌
docker-compose logs -f edge-all-in-one

# 停止
docker-compose down
```

---

## 模組化與升級策略

### 模組版本管理

```yaml
# module-versions.yaml
modules:
  mcp:
    version: "2.0.0"
    min_compatible: "1.5.0"
    dependencies:
      - llm_provider_manager: ">=1.0.0"
      - plugin_manager: ">=1.0.0"
  
  webui:
    version: "2.0.0"
    min_compatible: "1.8.0"
    dependencies:
      - mcp: ">=1.5.0"
  
  robot_console:
    version: "2.0.0"
    min_compatible: "1.5.0"
    dependencies: []
  
  llm_providers:
    ollama:
      version: "1.0.0"
      api_version: "0.1.17+"
    lmstudio:
      version: "1.0.0"
      api_version: "0.2.0+"
```

### 獨立升級流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     Module Upgrade Flow                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. 檢查更新                                                     │
│     • 連接更新伺服器                                             │
│     • 比對當前版本與可用版本                                     │
│     • 檢查相依性相容性                                           │
└────────────────────────────────────────────────────┬────────────┘
                                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. 下載更新                                                     │
│     • 下載差異更新包                                             │
│     • 驗證完整性（SHA256）                                       │
│     • 驗證簽章                                                   │
└────────────────────────────────────────────────────┬────────────┘
                                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. 備份當前版本                                                 │
│     • 備份模組檔案                                               │
│     • 備份配置                                                   │
│     • 記錄回滾點                                                 │
└────────────────────────────────────────────────────┬────────────┘
                                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. 套用更新                                                     │
│     • 停止相關服務                                               │
│     • 替換模組檔案                                               │
│     • 執行資料庫遷移（如需要）                                   │
│     • 重啟服務                                                   │
└────────────────────────────────────────────────────┬────────────┘
                                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. 驗證更新                                                     │
│     • 執行健康檢查                                               │
│     • 驗證功能正常                                               │
│     • 如失敗則自動回滾                                           │
└─────────────────────────────────────────────────────────────────┘
```

### 插件熱載入

```python
# 支援運行時載入新插件
class PluginHotLoader:
    """
    支援運行時熱載入插件
    """
    
    async def load_plugin(self, plugin_path: str) -> bool:
        """
        載入新插件而不重啟服務
        """
        # 驗證插件
        if not self._validate_plugin(plugin_path):
            return False
        
        # 載入插件
        plugin_class = self._import_plugin(plugin_path)
        
        # 註冊到插件管理器
        await self.plugin_manager.register_plugin(plugin_class)
        
        # 初始化插件
        await self.plugin_manager.initialize_plugin(plugin_class.name)
        
        return True
    
    async def unload_plugin(self, plugin_name: str) -> bool:
        """
        卸載插件而不重啟服務
        """
        await self.plugin_manager.shutdown_plugin(plugin_name)
        await self.plugin_manager.unregister_plugin(plugin_name)
        return True
```

---

## 安全性設計

### 本地安全機制

| 安全層級 | 機制 | 說明 |
|---------|------|------|
| **服務隔離** | 127.0.0.1 綁定 | 所有本地服務僅監聽回環介面 |
| **Token 認證** | Bearer Token | 服務間通訊使用短期 Token |
| **Context Isolation** | Electron Preload | 隔離渲染進程與主進程 |
| **檔案權限** | 嚴格權限 | 敏感檔案設定適當權限 |
| **密鑰儲存** | OS Keychain | 使用系統安全儲存 |

### 雲端通訊安全

| 安全層級 | 機制 | 說明 |
|---------|------|------|
| **傳輸加密** | TLS 1.3 | 所有雲端通訊使用 HTTPS |
| **身份認證** | JWT / OAuth 2.0 | 用戶身份驗證 |
| **API 金鑰** | 加密儲存 | 雲端 API 金鑰加密儲存 |
| **資料最小化** | 選擇性上傳 | 僅上傳用戶授權的資料 |

### 資料隱私

```yaml
# privacy-config.yaml
data_sharing:
  # 用戶可控制的分享設定
  share_command_history: false
  share_advanced_commands: true
  share_usage_analytics: true
  
  # 匿名化設定
  anonymize_user_id: true
  anonymize_robot_id: true
  
  # 資料保留
  local_history_days: 90
  cloud_sync_enabled: false
```

---

## 階段目標與驗收條件

### Phase 3.1 — 基礎整合（預估 4-6 週）

**目標**：完成 MCP + WebUI + Robot-Console 的基礎整合

**交付物**：
- [ ] 統一啟動器原型（Electron）
- [ ] 服務協調器（啟動/停止/健康檢查）
- [ ] 基礎 LLM 選擇介面
- [ ] 服務間狀態共享機制

**驗收條件**：
- [ ] 一鍵啟動所有服務
- [ ] 服務健康檢查通過
- [ ] 可選擇本地 LLM 提供商
- [ ] 基本指令執行流程完整

### Phase 3.2 — 功能完善（預估 4-6 週）

**目標**：完善 Edge 功能與用戶體驗

**交付物**：
- [ ] 完整的 WebUI 本地版
- [ ] 機器人監控儀表板
- [ ] 固件更新介面
- [ ] 離線模式支援
- [ ] CLI/TUI 版本

**驗收條件**：
- [ ] 離線模式下核心功能正常
- [ ] 固件更新流程完整
- [ ] CLI 可完整操作所有功能
- [ ] TUI 介面可用

### Phase 3.3 — 雲端整合（預估 4-6 週）

**目標**：整合雲端服務

**交付物**：
- [ ] 雲端同步機制
- [ ] 進階指令共享功能
- [ ] 用戶授權整合
- [ ] 共享 LLM 服務整合

**驗收條件**：
- [ ] 可上傳/下載進階指令
- [ ] 雲端用戶認證正常
- [ ] 本地/雲端 LLM 可切換
- [ ] 資料同步正確

### Phase 3.4 — 打包與發佈（預估 2-4 週）

**目標**：完成產品打包與發佈

**交付物**：
- [ ] Linux AppImage
- [ ] Windows 安裝程式
- [ ] macOS DMG
- [ ] Docker 映像
- [ ] 安裝文件

**驗收條件**：
- [ ] 各平台安裝程式可用
- [ ] Docker 部署正常
- [ ] 安裝流程文件完整

---

## 未來擴展

### Phase 4+ 規劃

| 功能 | 說明 | 優先級 |
|------|------|--------|
| **Whisper 整合** | 語音輸入支援 | 高 |
| **多機器人協調** | 多台機器人協同作業 | 中 |
| **ROS 2 整合** | 原生 ROS 2 支援 | 中 |
| **視覺 AI** | 影像辨識與導航 | 低 |
| **Kubernetes 部署** | 大規模部署支援 | 低 |
| **多租戶支援** | 企業級多用戶管理 | 低 |

### 擴展架構

```
┌─────────────────────────────────────────────────────────────────────┐
│                    Future Extensions                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐        │
│  │  Whisper ASR   │  │  Vision AI     │  │  ROS 2 Bridge  │        │
│  │  語音識別      │  │  視覺處理      │  │  ROS 整合      │        │
│  └───────┬────────┘  └───────┬────────┘  └───────┬────────┘        │
│          │                   │                   │                  │
│          └───────────────────┼───────────────────┘                  │
│                              │                                       │
│                              ▼                                       │
│          ┌───────────────────────────────────────────┐              │
│          │           Extension Manager               │              │
│          │  • 插件式架構                             │              │
│          │  • 動態載入/卸載                          │              │
│          │  • 版本相容管理                           │              │
│          └───────────────────────────────────────────┘              │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 參考文件

- [MASTER_PLAN.md](MASTER_PLAN.md) - 專案主計畫
- [PROJECT_MEMORY.md](../PROJECT_MEMORY.md) - 專案記憶與架構決策
- [MCP_LLM_PROVIDERS.md](../mcp/MCP_LLM_PROVIDERS.md) - LLM 提供商整合指南
- [MCP_PLUGIN_ARCHITECTURE.md](../mcp/MCP_PLUGIN_ARCHITECTURE.md) - 插件架構指南
- [architecture.md](../architecture.md) - 系統架構文件

---

**文件維護者**：開發團隊  
**下次審查**：Phase 3.1 完成時
