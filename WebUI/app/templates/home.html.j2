{% extends "base.html.j2" %}
{% block app_content %}
<div class="jumbotron">
  <h1>機器人指令中介層系統</h1>
  <p>本平台提供安全、可監控的人機/AI 指令下達與機器人管理，支援多種協定與即時監控。</p>
  {% if current_user.is_authenticated %}
    <a class="btn btn-primary btn-lg" href="{{ url_for('webui.robot_dashboard') }}">進入儀表板</a>
    <a class="btn btn-success btn-lg" href="{{ url_for('webui.register_robot') }}">註冊新機器人</a>
  {% else %}
    <a class="btn btn-primary btn-lg" href="{{ url_for('webui.login') }}">登入</a>
    <a class="btn btn-default btn-lg" href="{{ url_for('webui.register') }}">註冊帳號</a>
  {% endif %}
</div>
{% endblock %}
